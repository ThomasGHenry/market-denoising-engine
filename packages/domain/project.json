{
  "name": "domain",
  "tags": ["scope:domain"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["packages/domain/**/*.ts"]
      }
    }
  }
}
